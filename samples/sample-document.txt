RecallIQ Sample Document

This is a sample text document for testing the RecallIQ indexing pipeline.

It contains multiple paragraphs that will be chunked and embedded for semantic search.

The authentication guide for the internal API requires OAuth 2.0 tokens with JWT validation. All endpoints enforce role-based access control with three tiers: reader, editor, and administrator.

The invoice from April 2024 totals $12,450.00 for cloud infrastructure services. Payment terms are net-30 from the invoice date. The billing contact is accounts@example.com.

Kubernetes deployment manifests should be stored in the /k8s directory. The production cluster runs version 1.28 with Istio service mesh. Pod autoscaling is configured with HPA targeting 70% CPU utilization.
